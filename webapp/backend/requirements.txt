# Security-First RAG Web Application Dependencies
# All packages vetted for local-only operation with no external dependencies

# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database and ORM
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Vector database support
pgvector==0.2.4

# Authentication and security
python-jose[cryptography]==3.3.0
python-multipart==0.0.6
passlib[bcrypt]==1.7.4
bcrypt==4.1.2  # Python 3.12 compatible with passlib

# Async support
aiofiles==23.2.0
aiohttp==3.9.1

# Data processing (from existing requirements)
numpy>=1.26.0  # Python 3.12 compatible
scikit-learn==1.3.0
pandas==2.0.3

# Text processing and ML (existing)
beautifulsoup4==4.12.2
lxml==4.9.3
html5lib==1.1

# HTTP requests for Ollama communication
requests==2.31.0
httpx==0.25.2

# Configuration management
python-dotenv==1.0.0
pydantic[email]==2.5.0

# File handling
python-magic==0.4.27
filetype==1.2.0

# Logging and monitoring
structlog==23.2.0

# Development and testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Documentation
mkdocs==1.5.3
mkdocs-material==9.4.8

# Security scanning
bandit==1.7.5
safety==2.3.4

# Performance monitoring
memory-profiler==0.61.0
psutil==5.9.6

# Additional utilities
click==8.1.7
rich==13.7.0
typer==0.9.0

# WebSocket support
websockets==12.0
python-socketio==5.11.0

# Redis for Socket.IO session management (multi-worker support)
redis==5.0.1

# Local embedding models (sentence-transformers)
sentence-transformers==2.2.2
torch>=2.2.0  # Python 3.12 compatible
transformers==4.35.2

# vLLM for high-performance multi-GPU inference (OPTIONAL)
# Uncomment if using vLLM provider (LLM_PROVIDER=vllm)
# Requires CUDA and GPU support
# vllm>=0.6.0  # High-performance LLM serving with multi-GPU support
# ray>=2.9.0   # Required for vLLM distributed execution

# Optional: For PDF processing
PyPDF2==3.0.1
pdfplumber==0.10.3

# Image processing for PDF image extraction
Pillow==10.1.0

# Optional: For document processing
python-docx==1.1.0
openpyxl==3.1.2

# Date and time utilities
python-dateutil==2.8.2
pytz==2023.3

# Validation and serialization
marshmallow==3.20.1
cerberus==1.3.5

# Rate limiting
slowapi==0.1.9

# CORS and middleware
starlette==0.27.0

# Environment detection
python-decouple==3.8

# RAG Enhancement Dependencies (Added for 9 enhancements)

# 1. Reranker - Cross-encoder reranking with BGE-reranker-v2-m3
# FlagEmbedding==1.3.5  # Already included for BGE-M3 embeddings

# 2. Hybrid Search - BM25 + Vector ensemble retrieval
rank-bm25==0.2.2

# 3. Query Expansion & Corrective RAG - LangGraph workflow
langgraph==0.0.20
langchain-core==0.1.20

# 4. Web Search Fallback - DuckDuckGo for corrective RAG (optional, privacy-preserving)
duckduckgo-search==4.1.0

# 5. Evaluation Framework - RAGAS metrics for RAG quality assessment
ragas==0.1.1
datasets==2.16.1

# 6. Document Parsing Enhancement - Unstructured.io for complex layouts (OPTIONAL)
# unstructured[all-docs]==0.11.4  # Uncomment if needed for complex PDFs

# Note: All enhancements maintain local-first architecture
# - Reranker: Local BGE model (no external API)
# - Query Expansion: Local Ollama (Mistral/Llama2)
# - Hybrid Search: Local BM25 + local pgvector
# - Corrective RAG: Local LLM scoring, optional DuckDuckGo fallback (user-controlled)
# - Evaluation: Local RAGAS (no external services)
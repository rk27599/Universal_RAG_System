# Root-Level Dependencies for RAG System Development & Testing
# Note: webapp/backend has its own requirements.txt for production backend

# Core libraries (used by RAG system examples)
requests==2.31.0
beautifulsoup4==4.12.2
numpy>=1.26.0  # Python 3.12 compatible
scikit-learn==1.3.0

# Jupyter Notebook & Interactive Development (root-level only)
jupyter==1.0.0
notebook==7.0.6
ipykernel==6.25.0
matplotlib==3.7.2

# PyTorch 2.6.0+ with CUDA 12.4 (Required for BGE-M3 embeddings)
# Install with: pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# Embedding Models (for BGE-M3 migration scripts)
sentence-transformers==2.2.2  # Legacy support (all-MiniLM-L6-v2)
transformers==4.35.0
FlagEmbedding==1.3.5  # BGE-M3 embedding model (1024 dimensions)

# PDF Processing (for examples and testing)
PyMuPDF==1.26.5
pdfplumber==0.10.3
Pillow==10.1.0
nltk==3.9.2